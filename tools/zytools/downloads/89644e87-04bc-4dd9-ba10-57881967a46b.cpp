#include <iostream>
using namespace std;

int main() {
   int currentPrice;
   int lastMonthsPrice;
   
   cin >> currentPrice;
   cin >> lastMonthsPrice;
   
   mortgage = (currentPrice * 0.045) / 12 

   /* Type your code here. */
   cout << "This house is $" << currentPrice << ". The change is $-" <<lastMonthsPrice << "since last month." << endl;
   cout << "The estimated month morgage is $" << mortgage << "."

   return 0;
}
